{
  "name": "movie-catalog",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "prettier": "^3.1.0"
  },
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  }
}
